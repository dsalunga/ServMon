# Legacy IIS Express Artifacts

This folder contains archived IIS Express launcher/config artifacts that were previously stored at repository root.

- `IISExpress - ServMonWeb.cmd`
- `applicationhost.config`

These files are legacy references and are not used by the modern default build/run workflow.
